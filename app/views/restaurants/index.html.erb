<div class='container'>

  <h1>Restaurants</h1>
    <% @restaurants.each do |restau| %>
    <li>
      <%= link_to restau.name , restaurant_path(restau) %>
    </li>
  <% end %>

  <%= link_to 'Add a new restaurant', new_restaurant_path, class:"btn btn-primary"  %>
</div>
